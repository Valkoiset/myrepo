
path to this folder:
cd desktop/Python

# ---------------------------------------------------------------------------------------
Run python:
python/python3

Check python version:
python --version

Open Python shell:
idle/idle3

run current file in PyCharm on mac shortcut:
https://stackoverflow.com/questions/43807305/how-can-i-run-the-current-file-in-pycharm

guide for creating new environment in conda:
https://towardsdatascience.com/environment-management-with-conda-python-2-3-b9961a8a5097

conda create --name newenv python=3.6
source activate newenv

# ---------------------------------------------------------------------------------------
run Jupyter in certain directory:
jupyter notebook --notebook-dir=/Users/yourname/folder1/folder2/

set PATH to anaconda
export PATH="/Users/Valkoiset/anaconda3/bin:$PATH"

how should default clear $PATH variable look like:
export PATH=/usr/bin:/usr/sbin:/bin:/sbin


# environment with debugger set up
conda activate ml



# ------------------------------------------------------------------------
To change default running python when you type 'python' in console
A simple safe way would be to use an alias. Place this into ~/.bashrc or ~/.bash_aliases file:

alias python=python3
After adding the above in the file, run source ~/.bashrc or source ~/.bash_aliases.

For example:

$ python --version
Python 2.7.6
$ python3 --version
Python 3.4.3
$ alias python=python3
$ python --version
Python 3.4.3

alias idle="idle3"
alias pip='pip3'

You many find that a clean install of python3 actually points to python3.x so you may need:

alias pip='pip3.6'
alias python='python3.6'


check or tools in installed packages
conda list | grep ortools

# helped with or tools issue
pip install ortools==6.7.4973

### GIT ###
git add . # add all files to repo
git commit -m "some message" # commit these files with the message
git push # push these changes to github repo
git checkout -b draft # create new branch with the name "draft"
git branch # check what branch you currently are
git push --set-upstream origin draft 
# being in master it merges branch with master
git merge draft



### GIT ###
git add . # add all files to repo
git commit -m "some message" # commit these files with the message
git push # push these changes to github repo
git checkout -b draft # create new branch with the name "draft"
git branch # check what branch you currently are
git push --set-upstream origin draft 
# being in master it merges branch with master
git merge draft


